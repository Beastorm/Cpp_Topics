# OOP in C++ – Topic Breakdown

---

## 1. Core OOP Principles 🧩

- **Encapsulation**
- **Abstraction**
- **Inheritance**
- **Polymorphism**

---

## 2. Classes, Structs & Access Specifiers 🧱

### 2.1 Access Specifiers
- `public`
- `protected`
- `private`

### 2.2 `class` vs `struct`
- Default access (`class` → `private`, `struct` → `public`)
- Usage conventions (POD vs rich objects, etc.)

---

## 3. Constructors & Destructors 🛠

### 3.1 Constructors

- **Types of constructors:**
  - Default constructor
  - Parameterized constructor
  - Parameterized with default arguments
  - Copy constructor
    - Shallow copy
    - Deep copy
  - Move constructor
    - Lvalue vs Rvalue references
    - Move semantics

- **Initializer List**
  - Syntax
  - Benefits (performance, initialization of `const` & reference members, etc.)

### 3.2 Destructors

- Basic destructor
- Destructor behavior with inheritance  
  - `virtual` destructors and why they’re needed

---

## 4. Assignment & Operator Overloading ➕

### 4.1 Operator Overloading

- Unary operators
- Binary operators

### 4.2 Special Assignment Operators

- Copy assignment operator
- Move assignment operator

---

## 5. Friend Mechanism 🤝

- `friend` **function**
- `friend` **class**
- When to use `friend` & when not to
- Disadvantages of `friend` (tight coupling, encapsulation issues)

---

## 6. Abstract Classes & Interfaces 🧾

### 6.1 Abstract Classes

- Pure virtual functions
- Abstract base classes

### 6.2 Interfaces vs Abstract Classes

- Differences in intent & usage in C++

---

## 7. Value Categories & Lambdas ⚙️

- **Lvalues vs Rvalues**
  - Lvalue & Rvalue references
- **Lambda functions**
  - Captures, parameters, return types

---

## 8. Static, Qualifiers, Templates, Smart Pointers 📦

### 8.1 Static Members & Methods

- Static data members
- Static member functions

### 8.2 Qualifiers

- `const` / `volatile` (CV qualifiers)
- Reference qualifiers on member functions: `&`, `&&`

### 8.3 Templates (Generic OOP)

- Class templates
- Function templates

### 8.4 Smart Pointers (RAII & OOP)

- `std::unique_ptr`
- `std::shared_ptr`
- `std::weak_ptr`

---

## 9. Relationships & Polymorphism Details 🧬

### 9.1 Composition vs Aggregation

- “Has-a” relationships
- Ownership semantics

### 9.2 Overriding vs Overloading

- Function **overloading** (same name, different signature)
- Function **overriding** (same signature in derived class)

### 9.3 Virtual Mechanism

- `vtable`
- `vptr`
- `vbptr` (for virtual inheritance/base pointers)

### 9.4 Mixin Classes

- Using templates for behavior reuse (e.g., CRTP-based mixins)

---

## 10. Casting & Low-Level Details 🔍

### 10.1 Cast Operators

- `static_cast`
- `dynamic_cast`
- `const_cast`
- `reinterpret_cast`

### 10.2 Nested / Inner Classes

- Classes defined inside other classes

### 10.3 Enums

- Plain `enum`
- Scoped `enum class`

---

## 11. OOP-Related Keywords (Quick List) 📝

- `this`
- `new`, `delete`
- `default`, `final`
- `static`
- `mutable`
- `const`
- `explicit`
- `inline`
- `template`, `typename`
- `using`, `typedef`
- `namespace`
- `noexcept`

---

## 12. Important Idioms 💡

1. **RAII** (Resource Acquisition Is Initialization)
2. **PImpl** (Pointer to Implementation)
3. **Rule of Three / Five / Zero**
4. **Copy-and-Swap Idiom**
5. **CRTP** (Curiously Recurring Template Pattern)
6. **Virtual Constructor / Clone Idiom**
7. **Guard Idiom**
8. **Copy-on-Write (COW) Idiom**

---

## 13. Common Interview Questions (OOP in C++) 🎯

- **How many ways can we create objects in C++?**
  - (e.g., automatic, dynamic, static storage, placement new, etc.)

- **What happens if you try to override a function without marking it `virtual` in C++?**
  - (Name hiding vs true runtime polymorphism.)
